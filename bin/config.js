const config = {
    'template': {
        option: '-template,--template <n>',
        description: 'set template directory name',
        default: 'template',
        usage: 'create-code-template --template <n>'
    },
}

module.exports = config;